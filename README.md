Get-Content "trace.log" -Wait | Where-Object {($_ -notlike "*multipart/related*") -and ($_ -notlike "*Message contents*0000*'*") -and ($_ -notlike "*Content-Transfer-Encoding: binary*")} | Out-File "ws_trace.log" -Append -Encoding utf8
